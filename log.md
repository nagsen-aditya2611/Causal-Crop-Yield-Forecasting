# July 18th

## New updates

- Link to two datasets have been added in the data/raw/README.md file:
	- CY bench: The original dataset had data from over 25 countries, we filtered out one country (IN) and using that part only because other wise it'd be computationally impossible for us to do.
	- GDHY : Global Dataset of Historical Yield, which was hosted in the PANGAEA website.

## To be done next

- The GDHY data is not in CSV format and is in nc4 format, need to look into that
- Need to apply the causal discovery algorithms on these datasets
- Check if other datasets can also be viable for our work
- Form a DAG
- See if we can use any other algorithm except PCMCI

### Updated by Bhaskar
